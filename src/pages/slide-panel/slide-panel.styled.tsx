import styled from 'styled-components';
import { HoverAnimation } from '../../components/shared/shared.styled';

export const FavouritesListTitleClose = styled.i`
  font-size: 24px;
  float: right;
  cursor: pointer;
  ${HoverAnimation}
`;
